NOW:
Add GPU support.

NEXT:
Add tts-model.

LATER IDEAS:

Optimization:
    - Optimize encoder (audio chunk?).
    - qkv optimize.
    - flash attn.
    - Add NEON Optimization.

New features:
    - Implement tts-only (no microphone?, file_input? realtime?).
    - Add automatic voice detection activity (VAD)
    - Implement real-time tts and translation(Only multilingual models).
    - Submit prompt? Allow for prompt inspection before passing to llm.
    - Add output coloring.
